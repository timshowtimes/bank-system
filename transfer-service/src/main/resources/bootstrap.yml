spring:
  application:
    name: transfer-service
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 2000
